package labs.week12;

public class TestMain {
    public static void main(String[] args) {
        // Animal a = new Duck(); calisti
        // a.swim(); hata not compile and not run

        // a.fly(); hata not compile and not run

        // a.move(); calisti

        // Flyable f = (Flyable) new Fish(); compile but not run
        // f.fly(); compile but not run
    }
}
